//! QuestionAnswer seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial QuestionAnswer data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// QUESTIONANSWER SEEDER
// ============================================================================

/// Seeder for question_answer
pub struct SeedQuestionAnswerSeeder;

impl SeedQuestionAnswerSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedQuestionAnswerSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedQuestionAnswerSeeder {
    fn name(&self) -> &'static str {
        "SeedQuestionAnswerSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if survey.survey_question_answers table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM survey.survey_question_answers")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/question_answer_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM survey.survey_question_answers")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
