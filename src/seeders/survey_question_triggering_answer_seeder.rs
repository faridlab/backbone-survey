//! SurveyQuestionTriggeringAnswer seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial SurveyQuestionTriggeringAnswer data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SURVEYQUESTIONTRIGGERINGANSWER SEEDER
// ============================================================================

/// Seeder for survey_question_triggering_answer
pub struct SeedSurveyQuestionTriggeringAnswerSeeder;

impl SeedSurveyQuestionTriggeringAnswerSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSurveyQuestionTriggeringAnswerSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSurveyQuestionTriggeringAnswerSeeder {
    fn name(&self) -> &'static str {
        "SeedSurveyQuestionTriggeringAnswerSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if survey.survey_question_triggering_answers table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM survey.survey_question_triggering_answers")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/survey_question_triggering_answer_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM survey.survey_question_triggering_answers")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
