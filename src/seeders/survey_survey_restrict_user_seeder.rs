//! SurveySurveyRestrictUser seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial SurveySurveyRestrictUser data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SURVEYSURVEYRESTRICTUSER SEEDER
// ============================================================================

/// Seeder for survey_survey_restrict_user
pub struct SeedSurveySurveyRestrictUserSeeder;

impl SeedSurveySurveyRestrictUserSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSurveySurveyRestrictUserSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSurveySurveyRestrictUserSeeder {
    fn name(&self) -> &'static str {
        "SeedSurveySurveyRestrictUserSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if survey.survey_survey_restrict_users table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM survey.survey_survey_restrict_users")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/survey_survey_restrict_user_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM survey.survey_survey_restrict_users")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
