//! Application service for Survey entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `survey_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::presentation::dto::{CreateSurveyDto, UpdateSurveyDto};
use crate::domain::entity::Survey;
use crate::infrastructure::persistence::SurveyRepository;

/// Application service for Survey entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `survey_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct SurveyServiceCustom {
///     inner: Arc<SurveyService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type SurveyService = GenericCrudService<
    Survey,
    CreateSurveyDto,
    UpdateSurveyDto,
    SurveyRepository,
>;

// <<< CUSTOM
// END CUSTOM
