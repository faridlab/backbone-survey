//! Repository for UserInputLine entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<UserInputLine, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::UserInputLine;

/// Table name for UserInputLine entities
pub const TABLE_NAME: &str = "survey.survey_user_input_lines";

/// Repository for UserInputLine entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct UserInputLineRepository(
    backbone_orm::GenericCrudRepository<UserInputLine, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for UserInputLineRepository {
    type Target = backbone_orm::GenericCrudRepository<UserInputLine, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl UserInputLineRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(UserInputLineRepository, UserInputLine, soft_delete);
