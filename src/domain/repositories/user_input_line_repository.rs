//! Repository trait for UserInputLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UserInputLine aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{UserInputLine, SurveyAnswerType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserInputLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl UserInputLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct UserInputLinePaginatedResult {
    pub data: Vec<UserInputLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserInputLineFilter {
    pub user_input_id: Option<Uuid>,
    pub survey_id: Option<Uuid>,
    pub question_id: Option<Uuid>,
    pub suggested_answer_id: Option<Uuid>,
    pub matrix_row_id: Option<Uuid>,
    pub skipped: Option<bool>,
    pub answer_type: Option<SurveyAnswerType>,
    pub value_char_box: Option<String>,
    pub value_text_box: Option<String>,
    pub answer_is_correct: Option<bool>,
}

impl UserInputLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_input_id.is_some() || self.survey_id.is_some() || self.question_id.is_some() || self.suggested_answer_id.is_some() || self.matrix_row_id.is_some() || self.skipped.is_some() || self.answer_type.is_some() || self.value_char_box.is_some() || self.value_text_box.is_some() || self.answer_is_correct.is_some()
    }
}

/// Repository trait for UserInputLine aggregate
///
/// This trait defines the persistence contract for UserInputLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserInputLineRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user_input_line entity
    async fn save(&self, entity: &UserInputLine) -> Result<UserInputLine>;

    /// Find user_input_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UserInputLine>>;

    /// Find all user_input_line entities
    async fn find_all(&self) -> Result<Vec<UserInputLine>>;

    /// Update user_input_line by ID
    async fn update(&self, id: &str, entity: &UserInputLine) -> Result<Option<UserInputLine>>;

    /// Delete user_input_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user_input_line with pagination
    async fn list(&self, params: UserInputLinePaginationParams) -> Result<UserInputLinePaginatedResult>;

    /// List user_input_line with pagination and filters
    async fn list_with_filters(&self, params: UserInputLinePaginationParams, filters: UserInputLineFilter) -> Result<UserInputLinePaginatedResult>;

    /// Count all user_input_line entities
    async fn count(&self) -> Result<u64>;

    /// Count user_input_line entities matching filters
    async fn count_with_filters(&self, filters: UserInputLineFilter) -> Result<u64>;

    /// Check if user_input_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user_input_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user_input_line by ID
    async fn restore(&self, id: &str) -> Result<Option<UserInputLine>>;

    /// List soft-deleted user_input_line entities
    async fn list_deleted(&self, params: UserInputLinePaginationParams) -> Result<UserInputLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user_input_line entities
    async fn bulk_save(&self, entities: &[UserInputLine]) -> Result<Vec<UserInputLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
