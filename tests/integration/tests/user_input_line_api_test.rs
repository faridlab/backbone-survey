//! UserInputLine API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the UserInputLine CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// USERINPUTLINE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for UserInputLine
pub struct UserInputLineTestData;

impl TestDataGenerator for UserInputLineTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_input_id": Uuid::new_v4().to_string(),
            "survey_id": Uuid::new_v4().to_string(),
            "question_id": Uuid::new_v4().to_string(),
            "suggested_answer_id": null,
            "matrix_row_id": null,
            "skipped": false,
            "answer_type": null,
            "value_char_box": null,
            "value_text_box": null,
            "value_numerical_box": null,
            "value_scale": null,
            "value_date": null,
            "value_datetime": null,
            "answer_score": null,
            "answer_is_correct": null,
            "speed_seconds": null,
            "answered_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_input_id": Uuid::new_v4().to_string(),
            "survey_id": Uuid::new_v4().to_string(),
            "question_id": Uuid::new_v4().to_string(),
            "suggested_answer_id": null,
            "matrix_row_id": null,
            "skipped": false,
            "answer_type": null,
            "value_char_box": null,
            "value_text_box": null,
            "value_numerical_box": null,
            "value_scale": null,
            "value_date": null,
            "value_datetime": null,
            "answer_score": null,
            "answer_is_correct": null,
            "speed_seconds": null,
            "answered_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/survey_user_inputs", &super::user_input_api_test::UserInputTestData).await {
            deps.push(("user_input_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/survey_surveys", &super::survey_api_test::SurveyTestData).await {
            deps.push(("survey_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/survey_questions", &super::question_api_test::QuestionTestData).await {
            deps.push(("question_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// USERINPUTLINE API TEST
// ============================================================================

/// UserInputLine API test suite
pub struct UserInputLineApiTest {
    inner: GenericCrudTest<UserInputLineTestData>,
}

impl UserInputLineApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/survey_user_input_lines", "UserInputLine");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, UserInputLineTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for UserInputLineApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_user_input_line_crud() {
        let mut test = UserInputLineApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add UserInputLine specific tests here
// <<< CUSTOM TESTS END >>>
